# GreenHub-Crack

这个是GreenHub的app.asar和取码器

用法：先打开你安装GreenHub的地方，打开Resources

把app.asar替换

然后输错激活码，错时应该会弹出一个窗口，上边是Auth

编辑get.py，把auth（"Authorization": "Basic XXXXX"）这一段替换

然后用cmd运行get.exe

这时你的窗口上就会有一些激活码了，直接输。
